# Solver pages read gemini-api-key from <meta name="gemini-api-key" content="...">.
# Set GEMINI_API_KEY in env and have your host (server / Edge Function / build) inject it into the HTML.
GEMINI_API_KEY=
