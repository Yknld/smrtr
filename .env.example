# API keys (do not commit .env; copy to .env and fill in)
# Study OS web app: pass to solver iframe for Live Tutor (optional). Vite exposes VITE_* to client.
VITE_GEMINI_API_KEY=
# Solver / backend: inject GEMINI_API_KEY into solver HTML (meta tag) or use for edge functions.
GEMINI_API_KEY=

# RunPod (for TTS / lesson_generate_interactive, and scripts in gar/ or dont need/)
RUNPOD_API_KEY=
RUNPOD_ENDPOINT=

# Optional: Supabase project ref for setup-and-deploy scripts
# SUPABASE_PROJECT_REF=euxfugfzmpsemkjpcpuz
